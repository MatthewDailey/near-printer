// Note that React is necessary to use the jsx syntax here.
import React from 'react'
import RenderReactApp from './RenderReactApp'

import MapContainer from './MapContainer'

RenderReactApp(<MapContainer />)
